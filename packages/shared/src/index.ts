/**
 * Fidus Shared Package
 *
 * Shared types, schemas, and utilities used across Fidus packages.
 */

export const version = '0.1.0';

// Preference types and schemas
export type { Preference, PreferenceGroup } from './types/preference';
export { PreferenceSchema, PreferenceGroupSchema } from './schemas/preference';
